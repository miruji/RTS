
# Структура имеет a, b. Те соответственно имеют по два вложения внутри себя
files
  a
    "test1.rt"
    0
  b
    "test2.rt"
    -5

l = 1 # Это будет в качестве динамического выражения
# В данном случае мы проверияем динамическую ссылку со статическим значением
println(f"files.a.0 = {files.[0].0}:{type(files.[0].0)}")
# В данном случае мы проверяем динамическую ссылку с динамическим значением
println(f"files.b.0 = {files.[l].0}:{type(files.[l].0)}")

res = 0
? files.[0].0 != "test1.rt" # Проверяем на files.a.0 = "test1.rt"
  res = 1
? files.[l].0 != "test2.rt" # Проверяем на files.b.0 = "test2.rt"
  res = 1

# Если не было ошибок при проверке, то всё правильно
exit(res)