
ab(a: UInt, b: Int) -> UInt
  # Выводим a, b и их типы данных
  print(f"{a}:{type(a)} + {b}:{type(b)} ")
  = a + b

c = ab(11, -20) # Из-за того, что будет меньше 0 и тип UInt
println(f"= {c}:{type(c)}") # то результат 0

exit(c != 0) # Если c = 0 значит всё верно