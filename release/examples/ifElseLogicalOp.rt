
# RTS поддерживает:
  | как операция or
  & как операция and
  > а так же >=
  < а так же <=
  = проверка на равенство
  != проверка на неравенство
  ! проверка на нет (не работает на момент 241202)
  
  todo: RTS стремится к использованию полного диапазона условий логики;

a = 11 # const-UInt
b = 11 # const-UInt
? (a = 10 & b = 10) | (a-b != 10)
  println("or")
?
  println("else")